# -----------------------------------------------------------------------
# Cloudflare Pages redirects
#
# Host/protocol canonicalization is handled in functions/_middleware.ts.
# Keep this file free of active host-level rules; Cloudflare Pages only
# accepts relative source paths here, and invalid absolute host rules are
# ignored during deploy.
# -----------------------------------------------------------------------
